To launch the application:

1. Ensure you have Python and the required libraries installed (e.g., pandas, openpyxl, fpdf2, babel).
2. Open a terminal or command prompt in the project's root directory ("Window-Quotation").
3. Run the command: python -m __main__